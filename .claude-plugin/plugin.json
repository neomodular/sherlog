{
  "name": "sherlog",
  "version": "0.1.0",
  "description": "Hypothesis-driven debugging for Claude Code: the /debug detective loop drives suspects, discriminating probes, blocking reproduction waits, and guaranteed probe cleanup over a localhost daemon on :2218. Requires the sherlog binary on PATH — install it first with `brew install neomodular/tap/sherlog`."
}
